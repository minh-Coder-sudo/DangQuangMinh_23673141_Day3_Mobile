import { Pressable, StyleSheet, Text, View } from 'react-native';
const SIZE = 56;
export function FloatingCartButton({ count, onPress }: { count: number; onPress: () => void }) { return <Pressable accessibilityLabel="Thêm vào giỏ hàng" onPress={onPress} style={styles.button}><Text style={styles.icon}>🛒</Text>{count > 0 && <View style={styles.badge}><Text style={styles.badgeText}>{count}</Text></View>}</Pressable>; }
const styles = StyleSheet.create({ button: { alignItems: 'center', backgroundColor: '#4338CA', borderRadius: SIZE / 2, bottom: 24, elevation: 4, height: SIZE, justifyContent: 'center', position: 'absolute', right: 20, shadowColor: '#000', shadowOffset: { width: 0, height: 3 }, shadowOpacity: 0.2, shadowRadius: 6, width: SIZE }, icon: { fontSize: 22 }, badge: { alignItems: 'center', backgroundColor: '#DC2626', borderRadius: 10, height: 20, justifyContent: 'center', minWidth: 20, paddingHorizontal: 4, position: 'absolute', right: -4, top: -4 }, badgeText: { color: '#FFFFFF', fontSize: 11, fontWeight: '700' } });
